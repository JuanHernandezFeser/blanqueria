{
  "name": "blanqueria-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute blanqueria-db --remote --file=schema.sql",
    "db:seed": "wrangler d1 execute blanqueria-db --remote --file=seed.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250301.0",
    "wrangler": "^4.0.0"
  }
}
